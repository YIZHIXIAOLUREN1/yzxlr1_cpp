cpp，无注释和额外空格，换行少，尽量用来分隔不同的模块，但保留一定量的缩进，码风偏竞赛，变量名较简短，1-5个字符较为合适，使用以下基础模板并贴近码风与"\n"换行,数组下标从1开始，若过于复杂则使用scanf/printf而不用快读:
#include<bits/stdc++.h>
using namespace std;
#define ll long long
const int N=1e5+7;

void Solve(){
    
}

int main(){
    ios::sync_with_stdio(0);cin.tie(0);cout.tie(0);
    int T;
    //cin>>T;
    while(T--){
        Solve();
    }
}

c，无特殊情况遵循以下原则，无注释和额外空格，换行少，尽量用来分隔不同的模块，但保留一定量的缩进，码风偏竞赛，变量名较简短，1-4个字符合适，使用"\n"换行,数组下标从1开始。